/**
 * Logica di simulazione, manager e orchestrazione.
 * Importa tipi da `@/data` e aggiorna lo stato via `@/store` quando serve.
 */

export function placeholderEngineTick(_deltaMs: number): void {
  // hook per tick futuri (parametro ignorato per ora)
  void _deltaMs;
}
